import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  animatedView: {
    alignItems: 'center',
    marginBottom: 10,
    width: '100%',
  },
  buttonView: {
    alignItems: 'center',
    marginBottom: 20,
    marginTop: 40,
    width: '100%',
  },
  textInput: {
    marginTop: 20,
  },
});
